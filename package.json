{
  "name": "meeting-scheduler-react",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "cypress": "^3.4.1",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-scripts": "3.1.1"
  },
  "scripts": {
    "start": "HOST=0.0.0.0 && PORT=8000 && ./node_modules/.bin/react-scripts start",
    "pretest": "npm install",
    "build": "react-scripts build",
    "test": "rm -rf ./junit.xml && ./node_modules/.bin/cypress run"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
